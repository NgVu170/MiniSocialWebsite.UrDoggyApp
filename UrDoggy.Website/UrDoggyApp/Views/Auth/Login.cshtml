@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <title>Login - UrDoggy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #ffffff;
            font-family: 'Segoe UI', sans-serif;
        }

        .logo-box {
            background-color: #25D366;
            border-radius: 12px;
            padding: 10px 20px;
            display: inline-block;
            margin-bottom: 20px;
        }

            .logo-box img {
                height: auto;
                width: 200px;
            }

        .form-container {
            max-width: 400px;
            margin: 60px auto;
            text-align: center;
        }

        .form-control {
            border-radius: 8px;
        }

        .btn-primary-custom {
            background-color: #25D366;
            border: none;
            border-radius: 8px;
        }

        .divider {
            display: flex;
            align-items: center;
            text-align: center;
            margin: 20px 0;
        }

            .divider::before,
            .divider::after {
                content: '';
                flex: 1;
                border-bottom: 1px solid #ccc;
            }

            .divider span {
                margin: 0 10px;
                color: #999;
            }

        .google-btn {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
        }

            .google-btn img {
                height: 20px;
                margin-right: 10px;
            }

        .terms {
            font-size: 0.8rem;
            color: #666;
            margin-top: 20px;
        }

            .terms a {
                color: #25D366;
                text-decoration: none;
            }

                .terms a:hover {
                    text-decoration: underline;
                }
    </style>
</head>
<body>
    <div class="form-container">
        <div class="logo-box">
            <img src="~/images/logo.png" alt="UrDoggy Logo" />
        </div>

        <h3 class="text-success fw-bold">Welcome back</h3>
        <p class="text-success">Log in to your account</p>

        <form method="post">
            <input type="email" name="email" class="form-control mb-3" placeholder="Email address" required />
            <input type="password" name="password" class="form-control mb-3" placeholder="Password" required />
            <button type="submit" class="btn btn-primary-custom w-100 py-2">Log in</button>
        </form>

        <div class="mt-3">
            <span>Don't have an account? </span>
            <a href="/Auth/Register" class="text-success fw-semibold">Register now</a>
        </div>
        <div class="divider">
            <span>or continue with</span>
        </div>

        <button class="google-btn w-100">
            <img src="https://www.citypng.com/public/uploads/preview/google-logo-icon-gsuite-hd-701751694791470gzbayltphh.png" alt="Google Logo" />
            <span class="text-success fw-semibold">Google</span>
        </button>

        <div class="terms">
            By logging in, you agree to our
            <a href="#">Terms of Service</a> and
            <a href="#">Privacy Policy</a>.
        </div>
    </div>
</body>
</html>